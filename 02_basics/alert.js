// alert() is a built-in browser function that displays a modal dialog box with a message and an OK button.
// alert() is a blocking browser API that pauses JavaScript execution until the user dismisses it.
// It belongs to the browser environment, not core JS.

console.log("Before");
alert("Hi");
console.log("After");
