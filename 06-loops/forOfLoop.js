// for...of is used to iterate over values of an iterable (like arrays, strings, maps, sets).
// It gives you values directly, not indexes.

let fruits = ["mmango","apple","banana","litchi","orange"]

for(fruit of fruits){
    console.log(fruit);
}

// 

for(char of "apnacollege"){
    console.log(char)
}