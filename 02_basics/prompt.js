// prompt() is a built-in browser function that shows a modal dialog asking the user for input.
// It is part of the browser (window) API, not core JavaScript

const name = prompt("What is your name?");