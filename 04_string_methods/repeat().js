// repeat() is a string method that repeats a string multiple times and returns a new string.
// Original string is not changed

// string.repeat(count)
        // count → number of times to repeat
        // Must be 0 or a positive number

console.log("*".repeat(15));        